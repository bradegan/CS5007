// Write a C program that calls an add function(long add(long a, long b).

long add(long a, long b) {
    return a + b;
}

int main() {
    long a = 1;
    long b = 2;
    long c = add(a, b);
    return 0;
}
